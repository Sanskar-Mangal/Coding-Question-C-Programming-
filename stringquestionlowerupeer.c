#include <iostream>

using namespace std;

int main()
{
    string s;
    int l;
    cout<<"Enter the Name :";
    cin>>s;
    s.length();
    cout<<"string length :"<<s.length();
    for(int i=0;i<s.length();i++){
        if(i%==0){
            tolower()
        }
        else{
            toupper()
        }
    }
    cout<<"ans :"<<s;

    return 0;
}
